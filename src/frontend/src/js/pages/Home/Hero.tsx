import * as React from 'react';

import Container from '../../components/reusable/Container';
import Jumbotron from '../../components/reusable/Jumbotron';

const Hero = () => (
  <Jumbotron className="bg-hero bg-top bg-fixed bg-cover w-9/12"></Jumbotron>
);

export default Hero;
